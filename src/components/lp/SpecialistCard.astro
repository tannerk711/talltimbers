---
interface Props {
  variant?: 'compact' | 'full';
  theme?: 'dark' | 'light';
  name?: string;
  title?: string;
  nmls?: string;
  headshot?: string;
  brokerage?: string;
  brokerageLogo?: string;
  bio?: string;
  phoneNumber?: string;
  stats?: { label: string; value: string }[];
}

const {
  variant = 'compact',
  theme = 'dark',
  name = 'John Peisner',
  title = 'Florida DSCR Specialist',
  nmls = 'NMLS #239185',
  headshot = '/images/JP Headshot Final.jpg',
  brokerage = 'Barrett Financial Group',
  brokerageLogo = '/images/BFGLogo-Primary.png',
  bio = "John Peisner has structured 1,000+ DSCR loans at Barrett Financial Group, and he's the FL specialist real estate investors call when their first lender said no. Insurance, condo warrantability, STR zoning, all pre-run before your file ever hits underwriting.",
  phoneNumber = '(855) 738-1557',
  stats = [
    { label: 'FL Deals Closed', value: '1,000+' },
    { label: 'Avg Close', value: '14 Days' },
    { label: 'Lender Network', value: '50+' },
    { label: 'NMLS', value: '#239185' },
  ],
} = Astro.props;

const phoneHref = `tel:${phoneNumber.replace(/\D/g, '')}`;
const isDark = theme === 'dark';
---

{variant === 'compact' ? (
  <div class={`flex items-center gap-3 rounded-lg ${isDark ? 'bg-white/[0.04] border border-white/10' : 'bg-white border border-gray-200 shadow-sm'} p-3`}>
    <img
      src={headshot}
      alt={`${name}, ${title}`}
      class="h-12 w-12 rounded-full object-cover ring-2 ring-blue/30"
      loading="lazy"
      width="48"
      height="48"
    />
    <div class="flex-1 min-w-0">
      <div class={`text-xs uppercase tracking-wider ${isDark ? 'text-blue-light' : 'text-blue'} font-semibold`}>Your FL Specialist</div>
      <div class={`text-sm font-bold ${isDark ? 'text-white' : 'text-navy'} truncate`}>{name}</div>
      <div class={`text-xs ${isDark ? 'text-white/50' : 'text-gray-500'} truncate`}>{nmls} · {brokerage}</div>
    </div>
  </div>
) : (
  <div class="grid md:grid-cols-[280px_1fr] gap-8 items-start">
    <div class="relative">
      <img
        src={headshot}
        alt={`${name}, ${title}`}
        class="w-full max-w-[280px] aspect-square rounded-2xl object-cover shadow-lg"
        loading="lazy"
      />
      <div class="absolute -bottom-3 -right-3 hidden md:block bg-white rounded-xl shadow-md p-2 border border-gray-100">
        <img src={brokerageLogo} alt={brokerage} class="h-10 w-auto" />
      </div>
    </div>

    <div>
      <p class="text-xs font-medium uppercase tracking-[0.1em] text-amber mb-2">{title}</p>
      <h3 class="text-2xl md:text-3xl font-bold text-navy">{name}</h3>
      <p class="mt-1 text-sm text-gray-500">{nmls} · {brokerage}</p>

      <p class="mt-5 text-base leading-relaxed text-gray-600">{bio}</p>

      <div class="mt-6 grid grid-cols-2 sm:grid-cols-4 gap-4">
        {stats.map((s) => (
          <div class="rounded-lg bg-off-white border border-gray-100 px-3 py-3">
            <div class="text-xl font-extrabold text-navy">{s.value}</div>
            <div class="text-[11px] uppercase tracking-wider text-gray-500 mt-0.5">{s.label}</div>
          </div>
        ))}
      </div>

      <div class="mt-6 flex flex-col sm:flex-row gap-3">
        <a
          href="#lead-form"
          data-event="scroll_to_form"
          data-location="specialist_card"
          class="inline-flex items-center justify-center rounded-lg bg-blue px-5 py-3 text-sm font-semibold text-white transition-colors hover:bg-blue-dark"
        >
          Match Me With John
          <svg class="ml-2 h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
        </a>
        <a
          href={phoneHref}
          data-event="phone_click"
          data-location="specialist_card"
          class="inline-flex items-center justify-center gap-2 rounded-lg border border-gray-300 bg-white px-5 py-3 text-sm font-semibold text-navy transition-colors hover:border-blue hover:text-blue"
        >
          <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M3 5a2 2 0 012-2h2.28a1 1 0 01.95.68l1.5 4.49a1 1 0 01-.5 1.21l-1.86.93a11 11 0 005.2 5.2l.93-1.86a1 1 0 011.21-.5l4.49 1.5a1 1 0 01.68.95V19a2 2 0 01-2 2h-1C9.72 21 3 14.28 3 6V5z" />
          </svg>
          {phoneNumber}
        </a>
      </div>
    </div>
  </div>
)}
